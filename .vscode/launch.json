{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: start:prod",
      "request": "launch",
      "command": "yarn run start:prod",
      "cwd": "${workspaceFolder}"
    }
  ]
}